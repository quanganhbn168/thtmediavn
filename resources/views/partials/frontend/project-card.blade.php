<article class="case-card h-full">
    <a class="case-card__media" href="{{ route('projects.show', $project->slug) }}">@if($image = ($project->cover?->url ?: $project->getFirstMediaUrl('cover')))<img src="{{ $image }}" alt="{{ $project->getTranslation('name', 'vi') }}" loading="lazy">@else<span><i class="fa-solid fa-photo-film"></i></span>@endif</a>
    <div class="case-card__body"><div class="case-card__meta"><span>{{ $project->client?->getTranslation('name', 'vi') ?: 'Dự án THT Media' }}</span>@if($project->completed_year)<span>{{ $project->completed_year }}</span>@endif</div><h3><a class="transition-colors duration-200 hover:text-primary" href="{{ route('projects.show', $project->slug) }}">{{ $project->getTranslation('name', 'vi') }}</a></h3><p>{{ $project->getTranslation('summary', 'vi') }}</p><div class="case-card__tags">@foreach($project->services->take(3) as $service)<span>{{ $service->getTranslation('name', 'vi') }}</span>@endforeach</div><a class="inline-flex items-center gap-2 text-sm font-bold text-primary transition-colors duration-200 hover:text-secondary" href="{{ route('projects.show', $project->slug) }}">Xem chi tiết <i class="fa-solid fa-arrow-right text-xs" aria-hidden="true"></i></a></div>
</article>
