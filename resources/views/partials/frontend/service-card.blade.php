<article class="capability-card h-full">
    <a class="capability-card__media" href="{{ route('services.show', $service->slug) }}">
        @if($image = ($service->thumbnail?->url ?: $service->getFirstMediaUrl('thumbnail')))<img src="{{ $image }}" alt="{{ $service->getTranslation('name', 'vi') }}" loading="lazy">@else<span><i class="fa-solid fa-clapperboard"></i></span>@endif
    </a>
    <div class="capability-card__body"><span class="capability-card__group">{{ $service->category?->getTranslation('name', 'vi') ?: (\App\Models\Service::GROUPS[$service->group] ?? 'Dịch vụ') }}</span><h3><a href="{{ route('services.show', $service->getSlug('vi')) }}">{{ $service->getTranslation('name', 'vi') }}</a></h3><p>{{ $service->getTranslation('summary', 'vi') }}</p><div class="mt-auto flex flex-wrap gap-2"><a class="inline-flex min-h-10 items-center justify-center gap-2 rounded-full border border-primary bg-transparent px-4 py-2 text-xs font-bold leading-tight text-primary shadow-sm transition duration-200 hover:-translate-y-px hover:bg-primary-soft hover:text-primary hover:shadow-md focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary" href="{{ route('services.show', $service->getSlug('vi')) }}">Xem chi tiết</a><a class="inline-flex min-h-10 items-center justify-center gap-2 rounded-full border border-transparent px-4 py-2 text-xs font-bold leading-tight text-primary transition duration-200 hover:-translate-y-px hover:bg-primary-soft hover:text-primary-hover focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-primary" href="{{ route('contact', ['service' => $service->id]) }}">Nhận tư vấn</a></div></div>
</article>
