@extends('layouts.master')
@section('title', 'Combo')
@section('content')
<section class="section-space bg-soft"><div class="container"><div class="d-flex flex-wrap align-items-end justify-content-between gap-3 mb-4"><div><span class="eyebrow">GÓI SẢN PHẨM</span><h1 class="section-title mb-2">Combo</h1><p class="text-muted mb-0">Các nhóm sản phẩm được chuẩn bị theo từng nhu cầu sử dụng.</p></div><form action="{{ route('combos.index') }}" method="GET" class="d-flex gap-2"><input class="form-control" name="q" value="{{ $searchTerm }}" placeholder="Tìm Combo"><select class="form-select" name="sort"><option value="featured" @selected($sort === 'featured')>Nổi bật</option><option value="price-asc" @selected($sort === 'price-asc')>Giá tăng dần</option><option value="price-desc" @selected($sort === 'price-desc')>Giá giảm dần</option><option value="name-asc" @selected($sort === 'name-asc')>Tên A-Z</option></select><button class="btn btn-primary" type="submit">Lọc</button></form></div><div class="d-flex flex-wrap gap-2 mb-4"><a class="btn {{ $activeCategory ? 'btn-outline-secondary' : 'btn-primary' }}" href="{{ route('combos.index') }}">Tất cả</a>@foreach($categories as $category)<a class="btn {{ $activeCategory?->id === $category->id ? 'btn-primary' : 'btn-outline-secondary' }}" href="{{ route('combos.by-category', $category->slug) }}">{{ $category->name }}</a>@endforeach</div><div class="row g-4">@forelse($combos as $combo)<div class="col-6 col-md-4 col-xl-3"><x-combo-card :combo="$combo" /></div>@empty<div class="col-12"><div class="content-card text-center py-5"><h2 class="h5">Chưa có Combo phù hợp</h2><p class="text-muted mb-0">Các Combo đang được cập nhật.</p></div></div>@endforelse</div>@if($combos->hasPages())<div class="mt-4">{{ $combos->links() }}</div>@endif</div></section>
@endsection
