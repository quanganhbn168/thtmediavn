@extends('layouts.admin') @section('title','Đánh giá sản phẩm') @section('page-title','Duyệt đánh giá sản phẩm') @section('content')<x-admin.index-header description="Duyệt đánh giá sản phẩm trước khi hiển thị công khai."/><x-admin.filter-panel><form class="row g-3 align-items-end"><div class="col-md-7"><label class="form-label">Từ khóa</label><input name="search" value="{{ request('search') }}" class="form-control"></div><div class="col-md-3"><label class="form-label">Trạng thái</label><select name="status" class="form-select"><option value="">Tất cả</option>@foreach($statuses as $id=>$name)<option value="{{ $id }}" @selected(request('status')===$id)>{{ $name }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Lọc</button></div></form></x-admin.filter-panel><x-admin.table-card title="Danh sách đánh giá"><div class="table-responsive"><table class="table table-hover align-middle"><thead><tr><th>Khách hàng</th><th>Sản phẩm</th><th>Đánh giá</th><th>Nội dung</th><th>Kiểm duyệt</th><th></th></tr></thead><tbody>@forelse($reviews as $review)<tr><td><strong>{{ $review->name }}</strong>@if($review->is_verified)<small class="d-block text-success">Đã mua hàng</small>@endif</td><td>{{ $review->product?->name }}</td><td class="text-warning">{{ str_repeat('★',$review->rating) }}</td><td style="max-width:360px">{{ Str::limit($review->content,120) }}</td><td><form action="{{ route('admin.reviews.update',$review) }}" method="post" class="d-flex gap-2 align-items-center">@csrf @method('PATCH')<select name="status" class="form-select form-select-sm">@foreach($statuses as $id=>$name)<option value="{{ $id }}" @selected($review->status===$id)>{{ $name }}</option>@endforeach</select><label class="small"><input name="is_verified" value="1" type="checkbox" @checked($review->is_verified)> Xác minh</label><button class="btn btn-primary btn-sm">Lưu</button></form></td><td><form action="{{ route('admin.reviews.destroy',$review) }}" method="post">@csrf @method('DELETE')<button class="btn btn-default btn-sm text-danger"><i class="bi bi-trash"></i></button></form></td></tr>@empty<tr><td colspan="6" class="text-center py-5">Chưa có đánh giá.</td></tr>@endforelse</tbody></table></div>{{ $reviews->links() }}</x-admin.table-card>@endsection
