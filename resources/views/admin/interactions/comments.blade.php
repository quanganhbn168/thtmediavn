@extends('layouts.admin')
@section('title','Bình luận bài viết') @section('page-title','Duyệt bình luận bài viết')
@section('content')<x-admin.index-header description="Kiểm duyệt bình luận trước khi xuất hiện dưới bài viết."/><x-admin.filter-panel><form class="row g-3 align-items-end"><div class="col-md-7"><label class="form-label">Từ khóa</label><input name="search" value="{{ request('search') }}" class="form-control"></div><div class="col-md-3"><label class="form-label">Trạng thái</label><select name="status" class="form-select"><option value="">Tất cả</option>@foreach($statuses as $id=>$name)<option value="{{ $id }}" @selected(request('status')===$id)>{{ $name }}</option>@endforeach</select></div><div class="col-md-2"><button class="btn btn-primary w-100">Lọc</button></div></form></x-admin.filter-panel><x-admin.table-card title="Danh sách bình luận"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>Người gửi</th><th>Bài viết</th><th>Nội dung</th><th>Kiểm duyệt</th><th></th></tr></thead><tbody>@forelse($comments as $comment)<tr><td><strong>{{ $comment->name }}</strong><small class="d-block text-body-secondary">{{ $comment->email }}</small></td><td>{{ Str::limit($comment->post?->name??'—',40) }}</td><td style="max-width:420px">{{ Str::limit($comment->content,160) }}</td><td><form action="{{ route('admin.comments.update',$comment) }}" method="POST" class="d-flex gap-2">@csrf @method('PATCH')<select name="status" class="form-select form-select-sm">@foreach($statuses as $id=>$name)<option value="{{ $id }}" @selected($comment->status===$id)>{{ $name }}</option>@endforeach</select><button class="btn btn-primary btn-sm">Lưu</button></form></td><td><button form="delete-comment-{{ $comment->id }}" class="btn btn-default btn-sm text-danger"><i class="bi bi-trash"></i></button></td></tr>@empty<tr><td colspan="5" class="text-center py-5">Chưa có bình luận.</td></tr>@endforelse</tbody></table></div>@foreach($comments as $comment)<form id="delete-comment-{{ $comment->id }}" action="{{ route('admin.comments.destroy',$comment) }}" method="POST" class="d-none" data-admin-delete-form>@csrf @method('DELETE')</form>@endforeach<x-slot:footer>{{ $comments->links() }}</x-slot:footer></x-admin.table-card>@endsection
